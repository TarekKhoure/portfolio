import ContactForm from "../../components/ContactForm";

const ContactSection = () => {
  return (
    <section id="Contact">
      <ContactForm />
    </section>
  );
};

export default ContactSection;
